<header class="header user">
  <ul class="nav">
    <li>
      <a href="/">
      <i class="fas fa-home"></i>
      <span class="menu-items">Home</span>
    </a>
    </li>
    <% if(user){ %>
      
      <%/* <li> %>
        <a href="#">
          <i class="fas fa-book-open"></i>
          <span class="menu-items">Cart</spna> 
        </a>
      </li> %>
      <% <li><a href="/authors/books">Authors</a></li> %>
      <% <li><a href="/authors">Add Author</a></li> */%>

      <% if(user.image){ %>
        <li>
          <img class="image" src="<%= user ? user.image : "" %>" alt="" />
          <p class="imgcap"><%= user ? user.name : "" %></p>
        </li>
      <% } %>

      <li class="logout-sec" >
        <div class="img-cont">
          <span class="user-img"><%= user ? user.name.slice(0,1) : "" %></span>
          <p class="imgcap"><%= user ? user.name.split(" ").slice(0,1) : "" %></p>
        </div>

        <a href="/users/logout">
          <i class="fas fa-sign-out-alt"></i>
          <span class="menu-items">Logout</span>
        </a>
      </li>
    <%} else { %>
      <li>
        <a href="/auth" >
          <i class="fas fa-user"></i>
          <span class="menu-items">Author</span>
        </a>
      </li>
      <li>
        <a href="/users/login">
          <i class="fas fa-sign-in-alt"></i>
          <span class="menu-items">SignIn</span>
        </a>
      </li>
      <li>
        <a href="/users/register">
          <i class="fas fa-sign-out-alt"></i>
          <span class="menu-items">SignUp</span>
        </a>
      </li>
    <% } %>
  </ul>
</header>